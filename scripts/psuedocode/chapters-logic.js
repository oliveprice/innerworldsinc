
 // // array of all chapters with their content
 // make a list of all chapter sections and what goes in them
   // {
     // id: 'chapter-1',
     // blockImage: './resources/animations/chapter-1/images/img_12.png',
     // flowerLottie: './resources/animations/chapter-1/images/img_1.png',
     // chapterNumber: '1',
     // chapterName: 'CODE RED',
     // lineImage: './resources/animations/chapter-1/images/img_7.png'
   // },
   // // add more here...
 // ];

 // this is a helper called 'createChapterHTML' that handles part of the animation
   // create a new piece of the page
   // assign it a class so we can style it or target it later
   // define something we're going to use

   // create a new piece of the page
   // assign it a class so we can style it or target it later

   // create a new piece of the page
   // assign it a class so we can style it or target it later
   // define something we're going to use
   // define something we're going to use

   // create a new piece of the page
   // assign it a class so we can style it or target it later

   // define something we're going to use
     // { name: 'Kody Joliet', number: '1' },
     // { name: 'Code Red', number: '2' },
     // { name: 'r8r.world', number: '3' }
   // ];

   // define something we're going to use
     // define something we're going to use
     // attach this thing to whatever comes before it
   // });

   // attach this thing to whatever comes before it
   // attach this thing to whatever comes before it
   // attach this thing to whatever comes before it

   // send this thing back to be used somewhere else
 // }

 // this is a helper called 'createPageNode' that handles part of the animation
   // create a new piece of the page
   // assign it a class so we can style it or target it later

   // create a new piece of the page
   // assign it a class so we can style it or target it later

   // create a new piece of the page
   // assign it a class so we can style it or target it later
   // add the actual text that should show up in this spot

   // create a new piece of the page
   // define something we're going to use

   // attach this thing to whatever comes before it
   // attach this thing to whatever comes before it

   // create a new piece of the page
   // assign it a class so we can style it or target it later

   // create a new piece of the page
   // assign it a class so we can style it or target it later
   // add the actual text that should show up in this spot

   // attach this thing to whatever comes before it

   // create a new piece of the page
   // define something we're going to use
   // assign it a class so we can style it or target it later
   // apply a visual style to this thing
   // apply a visual style to this thing
   // set up how this should animate in or out
   // attach this thing to whatever comes before it

   // attach this thing to whatever comes before it
   // attach this thing to whatever comes before it
   // send this thing back to be used somewhere else
 // }

 // // ENTRY POINT: When user hovers a chapter
 // this is a helper called 'initChapterReveal' that handles part of the animation
   // grab a specific element or group of elements from the page

   // apply a visual style to this thing
   // set up how this should animate in or out

   // wait for the user to do something like hover
     // startChapterSequence(container, chapterBlock);
   // });

   // wait for the user to do something like hover
     // slideChapterBlockIn(chapterBlock);
   // });
 // }

 // // LEVEL 1: Chapter hover → trigger whole sequence
 // this is a helper called 'startChapterSequence' that handles part of the animation
   // slideChapterBlockOut(chapterBlock);

   // wait a little bit before doing the next thing
     // revealPageNodesInOrder(container);
   // }, 500);
 // }

 // // LEVEL 2: Stagger in page nodes, one per second
 // this is a helper called 'revealPageNodesInOrder' that handles part of the animation
   // grab a specific element or group of elements from the page

   // define something we're going to use
     // wait a little bit before doing the next thing
       // revealPageNodeInSequence(node);
     // }, i * 500);
   // });
 // }

 // // LEVEL 3: Animate a single page node’s elements
 // this is a helper called 'revealPageNodeInSequence' that handles part of the animation
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
 // }

 // // BASIC ELEMENT ANIMATIONS
 // this is a helper called 'animateFlower' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (flower) {
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'animateNumber' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (number) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'reverseNumber' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (number) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }


 // this is a helper called 'animateLine' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (line) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'reverseLine' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (line) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }


 // this is a helper called 'animateText' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (text) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'reverseText' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (text) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }


 // this is a helper called 'slideChapterBlockOut' that handles part of the animation
   // apply a visual style to this thing
 // }

 // this is a helper called 'slideChapterBlockIn' that handles part of the animation
   // apply a visual style to this thing
 // }
 // // Reverse all page nodes in order, from last to first
 // this is a helper called 'reversePageNodesInOrder' that handles part of the animation
   // grab a specific element or group of elements from the page

   // define something we're going to use
     // wait a little bit before doing the next thing
       // reversePageNodeSequence(node);
     // }, i * 500);
   // });
 // }

 // // Reverse animation of one page node's parts
 // this is a helper called 'reversePageNodeSequence' that handles part of the animation
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
   // wait a little bit before doing the next thing
 // }

 // // Reverse flower
 // this is a helper called 'reverseFlower' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (flower) {
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // // Reverse number
 // this is a helper called 'reverseNumber' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (number) {
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'animateLine' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (line) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // this is a helper called 'reverseLine' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (line) {
     // set up how this should animate in or out
     // apply a visual style to this thing
     // apply a visual style to this thing
   // }
 // }

 // // Reverse text
 // this is a helper called 'reverseText' that handles part of the animation
   // grab a specific element or group of elements from the page
   // if (text) {
     // apply a visual style to this thing
   // }
 // }

 // // 🧠 ADD: Setup initial state for all pieces on load
 // this is a helper called 'setInitialState' that handles part of the animation
   // grab a specific element or group of elements from the page
   // define something we're going to use
     // grab a specific element or group of elements from the page
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
     // apply a visual style to this thing
   // });
 // }

 // // MODIFIED: initChapterReveal to include reverse trigger
 // this is a helper called 'initChapterReveal' that handles part of the animation
   // grab a specific element or group of elements from the page

   // apply a visual style to this thing
   // set up how this should animate in or out

   // setInitialState(container); // <-- new

   // wait for the user to do something like hover
     // startChapterSequence(container, chapterBlock);
   // });

   // wait for the user to do something like hover
     // reversePageNodesInOrder(container);
     // wait a little bit before doing the next thing
       // slideChapterBlockIn(chapterBlock);
     // }, 500 * 3); // after 3 page nodes reverse
   // });
 // }

 // // init
 // define something we're going to use
   // define something we're going to use
   // attach this thing to whatever comes before it
   // initChapterReveal(chapterEl);
 // });